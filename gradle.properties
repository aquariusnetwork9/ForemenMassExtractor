org.gradle.jvmargs=-Xmx2G
org.gradle.parallel=true
# Loom 1.8 + Gradle 8.10 must run on JDK <= 21 (the system JDK here is 24, which Gradle
# 8.10 rejects). Pin the Gradle runtime to the local JDK 21. Machine-specific — adjust the
# path or remove this line if building elsewhere.
org.gradle.java.home=C:/Program Files/Java/jdk-21

# Fabric Properties (https://fabricmc.net/develop)
minecraft_version=1.21.4
yarn_mappings=1.21.4+build.8
loader_version=0.16.9

# Mod Properties
mod_version=1.2.5
maven_group=com.shallowplague.foreman
archives_base_name=foreman-massextractor
